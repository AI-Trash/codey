# Flows
HEADLESS=false
SLOW_MO=0
OPENAI_BASE_URL=https://openai.com
CHATGPT_URL=https://chatgpt.com
DEFAULT_TIMEOUT_MS=15000
NAVIGATION_TIMEOUT_MS=30000

# ChatGPT Team trial checkout billing address. Required when the
# chatgpt-team-trial flow reaches checkout unless the same values are passed
# through CLI flags or dispatch config.
# CHATGPT_TEAM_TRIAL_BILLING_NAME=Your billing name
# CHATGPT_TEAM_TRIAL_BILLING_COUNTRY=IE
# CHATGPT_TEAM_TRIAL_BILLING_ADDRESS_LINE1=Your street address
# CHATGPT_TEAM_TRIAL_BILLING_ADDRESS_LINE2=
# CHATGPT_TEAM_TRIAL_BILLING_CITY=Dublin
# CHATGPT_TEAM_TRIAL_BILLING_STATE=
# CHATGPT_TEAM_TRIAL_BILLING_POSTAL_CODE=D02 XXXX
#
# GoPay trial continuation. The phone number is required when --claimTrial
# gopay reaches the Midtrans tokenization page. PIN is optional; if omitted,
# the flow opens the GoPay authorization page and waits for manual completion
# until the timeout.
# CHATGPT_TEAM_TRIAL_GOPAY_COUNTRY_CODE=86
# CHATGPT_TEAM_TRIAL_GOPAY_PHONE_NUMBER=18400000000
# CHATGPT_TEAM_TRIAL_GOPAY_PIN=
# CHATGPT_TEAM_TRIAL_GOPAY_AUTHORIZATION_TIMEOUT_MS=180000

# Optional CLI/browser proxy. If unset, Codey will use the enabled Windows
# system proxy when available. Explicit values win over system proxy.
# CODEY_PROXY_URL=http://127.0.0.1:7890
# CODEY_PROXY_BYPASS=localhost,127.0.0.1,::1
# CODEY_USE_SYSTEM_PROXY=true
#
# Optional managed proxy nodes. In remote worker mode, Codey can fetch enabled
# nodes from /admin/proxy-nodes, start a local sing-box mixed inbound, and route
# browser traffic through it without changing the system proxy.
# CODEY_SINGBOX_ENABLED=true
# CODEY_SINGBOX_EXECUTABLE=sing-box
# CODEY_SINGBOX_MIXED_HOST=127.0.0.1
# CODEY_SINGBOX_MIXED_PORT=2080
# CODEY_SINGBOX_DEFAULT_TAG=japan

# PostgreSQL database
# DATABASE_URL=postgresql://codey:codey@localhost:5432/codey

# Exchange / Microsoft Graph (optional)
# EXCHANGE_TENANT_ID=your-tenant-id
# EXCHANGE_CLIENT_ID=your-app-client-id
# EXCHANGE_CLIENT_SECRET=your-app-client-secret
# EXCHANGE_MAILBOX=user@contoso.com
# EXCHANGE_CATCH_ALL_PREFIX=codey

# App
# APP_BASE_URL=http://localhost:3000
# SESSION_COOKIE_NAME=codey_session
# SESSION_TTL_DAYS=14

# GitHub OAuth for browser admin login
# GITHUB_CLIENT_ID=your-github-client-id
# GITHUB_CLIENT_SECRET=your-github-client-secret
# GITHUB_SCOPE=read:user user:email
# ADMIN_GITHUB_LOGINS=octocat,hubot
# FLOW_APP_API_KEY=replace-with-a-long-random-secret
# FLOW_APP_API_KEY_HEADER=x-codey-flow-app-key

# Verification API access
# VERIFICATION_API_KEY=shared-app-client-key
# VERIFICATION_API_KEY_HEADER=x-codey-api-key

# App-backed verification mailbox generation
# Verification domains are managed from /admin/domains and randomly selected
# from the enabled domain pool during CLI registration.
# VERIFICATION_EMAIL_PREFIX=codey
# VERIFICATION_RESERVATION_TTL_MINUTES=15
# Legacy single-domain bootstrap (optional compatibility seed only)
# VERIFICATION_MAILBOX=codey@your-domain.com
# VERIFICATION_DOMAIN=your-domain.com

# Device authorization
# DEVICE_CHALLENGE_TTL_MINUTES=15

# Cloudflare Email Worker -> app webhook verification
# CLOUDFLARE_EMAIL_WEBHOOK_SECRET=replace-with-a-long-random-secret
# CLOUDFLARE_SIGNATURE_HEADER=x-codey-signature
# CLOUDFLARE_TIMESTAMP_HEADER=x-codey-timestamp

# Standards-based OAuth / OIDC backend foundation
# OAUTH_ISSUER=http://localhost:3000
# OAUTH_JWKS_JSON={"keys":[{"kty":"RSA","e":"AQAB","n":"...","d":"...","p":"...","q":"...","dp":"...","dq":"...","qi":"...","kid":"codey-seed","use":"sig","alg":"RS256"}]}
# OAUTH_JWKS_JSON is optional and only used as a one-time seed if the signing-key table is empty.
# Also used to encrypt shared ChatGPT identity credentials stored by Codey app.
# OAUTH_CLIENT_SECRET_ENCRYPTION_KEY=base64-encoded-32-byte-key
# OAUTH_ACCESS_TOKEN_TTL_SECONDS=3600
# OAUTH_DEVICE_CODE_TTL_SECONDS=600
# OAUTH_SIGNING_KEY_ROTATION_DAYS=30
# OAUTH_SIGNING_KEY_RETENTION_DAYS=7
# OAUTH_DEFAULT_RESOURCE_INDICATOR=http://localhost:3000
# OAUTH_SUPPORTED_SCOPES=flow-app-requests:write,notifications:read,verification:ingest,verification:read,verification:reserve

# Shared Codey app client for flows and CLI auth
# If CODEY_APP_CLIENT_SECRET is set, verification flows use client_credentials.
# If it is omitted, verification flows fall back to device code authorization.
# CODEY_APP_BASE_URL=http://localhost:3000
# CODEY_APP_OIDC_ISSUER=http://localhost:3000/oidc
# CODEY_APP_OIDC_BASE_PATH=/oidc
# CODEY_APP_CLIENT_ID=your-codey-client-id
# CODEY_APP_CLIENT_SECRET=your-codey-client-secret
# CODEY_APP_RESOURCE=http://localhost:3000
# CODEY_APP_TOKEN_ENDPOINT_AUTH_METHOD=client_secret_basic
# CODEY_APP_CLI_EVENTS_PATH=/api/cli/events
# CODEY_APP_DEVICE_START_PATH=/api/device
# CODEY_APP_DEVICE_STATUS_PATH=/api/device/{deviceCode}
# CODEY_APP_DEVICE_EVENTS_PATH=/api/device/{deviceCode}/events
# CODEY_APP_RESERVE_EMAIL_PATH=/api/verification/email-reservations
# CODEY_APP_CODE_PATH=/api/verification/codes
# CODEY_APP_EVENTS_PATH=/api/verification/events
# CODEY_APP_WHATSAPP_NOTIFICATION_INGEST_PATH=/api/ingest/whatsapp-notification

# Android WhatsApp notification watcher (CLI auto-start)
# ANDROID_WHATSAPP_WATCH_ENABLED=true
# Optional: override adb path when Android Studio SDK is not in the default location
# ANDROID_ADB_PATH=C:\Users\you\AppData\Local\Android\Sdk\platform-tools\adb.exe
# ANDROID_UDID=emulator-5554
# ANDROID_FRIDA_SERVER_PATH=C:\path\to\frida-server
# ANDROID_FRIDA_AUTO_DOWNLOAD=true
# ANDROID_FRIDA_DOWNLOAD_DIR=C:\Users\you\.codey\frida
# ANDROID_FRIDA_REMOTE_PATH=/data/local/tmp/frida-server
# ANDROID_FRIDA_SERVER_PORT=27042
# ANDROID_FRIDA_START_SERVER=true
# ANDROID_FRIDA_TARGET=system_server
# ANDROID_WHATSAPP_PACKAGES=com.whatsapp,com.whatsapp.w4b

# Codex OAuth (optional)
# Built-in defaults match OpenAI; uncomment only to override them.
# CODEX_AUTHORIZE_URL=https://auth.openai.com/oauth/authorize
# CODEX_TOKEN_URL=https://auth.openai.com/oauth/token
# CODEX_CLIENT_ID=app_EMoamEEZ73f0CkXaXp7hrann
# CODEX_CLIENT_SECRET=your-codex-client-secret
# CODEX_SCOPE=openid profile email offline_access
# CODEX_REDIRECT_HOST=localhost
# CODEX_REDIRECT_PORT=1455
# CODEX_REDIRECT_PATH=/auth/callback

# Sub2API sync for Codex OAuth sessions is configured in /admin/external-services.
# Codey Web keeps those credentials on the server and performs the Sub2API sync
# after the managed Codex session is saved.
# For standalone CLI-only sync, SUB2API_OPENAI_OAUTH_RESPONSES_WEBSOCKET_V2_MODE
# may be set to off, ctx_pool, or passthrough.

# Identity maintenance dispatches low-priority chatgpt-login tasks to connected
# CLIs when they have spare browser capacity. Workspace owner identities are not
# maintained by this background path.
# IDENTITY_MAINTENANCE_ENABLED=true
# IDENTITY_MAINTENANCE_SCHEDULER_INTERVAL_MS=60000
# IDENTITY_MAINTENANCE_MIN_INTERVAL_MS=43200000
# IDENTITY_MAINTENANCE_MAX_ASSIGNED_TASKS_PER_CLI=0
# IDENTITY_MAINTENANCE_MIN_IDLE_BROWSER_SLOTS=0
# IDENTITY_MAINTENANCE_MAX_TASKS_PER_CLI=1
# IDENTITY_MAINTENANCE_MAX_TASKS_PER_TICK=3

# AstrBot proactive PayPal link notification is configured in
# /admin/external-services. No ASTRBOT_* environment variables are required.
